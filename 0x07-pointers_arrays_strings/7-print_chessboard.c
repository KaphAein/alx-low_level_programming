#include "main.h"

/**
 * _print_chessboard - function that takes elements
 * @a: element
 *
 * Return: void
 */

void print_chessboard(char (*a)[8])
{
	int i = 0;

	while (a[i] != '\0')
	{
		_putchar ("a[i]");
		i++;
}
