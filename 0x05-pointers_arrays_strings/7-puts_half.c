#include "main.h"

/**
* puts_half - function that takes a pointer
* @str: pointer
* Return: void
*/

void puts_half(char *str);
{
	int i, size;

	i = 0;
	while (s[i] != '\0')
	{
		i++;
	}

	size = --i;
	i = 0;
	while (str[i] != '\0')
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
